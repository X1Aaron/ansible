---
- name: User Management Playbook
  hosts: localhost
  become: yes
  vars_files:
    - ../vars/users.yml
    # Local overrides (not tracked in git - safe to edit on server)
    # This file will override values from users.yml if it exists
    - ../vars/users.local.yml
  
  tasks:
    - name: Include user management role
      include_role:
        name: user_management
      vars:
        action: "{{ user_action | default('create') }}"

