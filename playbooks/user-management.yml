---
- name: User Management Playbook
  hosts: localhost
  become: yes
  vars_files:
    # Load ALL your user variables from this ONE file
    # Edit vars/users.local.yml and uncomment/modify the examples
    - ../vars/users.local.yml
  
  roles:
    - role: user_management

