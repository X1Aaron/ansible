---
# User Management Variables - TEMPLATE FILE
# 
# ⚠️  DO NOT EDIT THIS FILE! ⚠️
# 
# This file is tracked in git and will be overwritten when you sync.
# 
# Instead, create and edit: vars/users.local.yml
# 
# Quick start:
#   cp vars/users.local.yml.example vars/users.local.yml
#   nano vars/users.local.yml
# 
# The vars/users.local.yml file is NOT tracked in git and will never be overwritten.
# Put ALL your user definitions there - it's the only file you need to edit!

# Users to create
users_to_create:
  # - name: john_doe
  #   groups: ['sudo', 'docker']
  #   shell: /bin/bash
  #   comment: "John Doe - Developer"
  #   create_home: true
  #   generate_ssh_key: true
  #   ssh_key_type: rsa
  #   ssh_key_bits: 2048
  #   password: "{{ user_passwords['john_doe'] | default(omit) }}"  # Reference from vault/passwords.yml
  #   password_lock: false

# Users to modify
users_to_modify:
  # - name: existing_user
  #   groups: ['sudo']
  #   append: true
  #   shell: /bin/zsh
  #   comment: "Updated comment"

# Users to delete
users_to_delete:
  # - old_user1
  # - old_user2

# Options for delete action
remove_home: false  # Set to true to remove home directory when deleting user
force_remove: false  # Set to true to force removal even if user is logged in

